#summary Lists stuff we need to do, along with who's doing it.

= To Do =
Functions we need to implement:
|| function || who's doing it ||
|| open || ||
|| read || ||
|| write || ||
|| lseek || ||
|| close || ||
|| dup2 || ||
|| getpid || ||
|| fork || ||
|| execv || ||
|| waitpid || ||
|| `_`exit || ||